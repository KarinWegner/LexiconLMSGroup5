@if (courseEntries != null && courseEntries.Count() != 0)
{
    @foreach (var e in courseEntries)
    {
        <CourseDashboardEntry CourseEntry="e" CourseEntryType="CourseEntryType"></CourseDashboardEntry>
    }
}