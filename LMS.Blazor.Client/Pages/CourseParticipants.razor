@attribute [Route(VBRoutes.Student.LinkToCourseParticipants)]
@rendermode InteractiveWebAssembly

<h3>Course Participants</h3>

<CourseNavbar ActiveNav="ECourseNav.CourseParticipants"></CourseNavbar>
@if (Participants != null && Participants.Count() != 0)
{
    <CourseParticipantList ListEntries="@Participants"></CourseParticipantList>
}